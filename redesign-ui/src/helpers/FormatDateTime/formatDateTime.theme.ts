import { FormatDateTimeInterface } from './formatDateTime.types';

export function formatDateTimeTheme({}: // theme: { transition, color, unit },
FormatDateTimeInterface) {
  return {};
}

export function formatDateTimeAppearanceTheme({}: // appearance,
// theme: { border, color, unit },
FormatDateTimeInterface) {
  return {};
}
