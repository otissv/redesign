import { SectionInterface } from './section.types';

export function sectionTheme({}: SectionInterface) {
  return {
    padding: '10px 12px',
    display: 'flex',
    flexDirection: 'column',
  };
}
