import { ThemeComponentInterface } from '@redesign/theme'

export interface ListInterface extends ThemeComponentInterface {
  columns?: boolean
}

export interface ListItemInterface extends ThemeComponentInterface {}
